name: test-vault
on: push
runs-on: [self-hosted, Linux, X64]
permissions:
  id-token: write
  contents: read
jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: Zaloguj do Vault przez OIDC
        run: |
          GH_TOKEN=$(curl -sSL -H "Authorization: bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" \
            "$ACTIONS_ID_TOKEN_REQUEST_URL" | jq -r .value)
          VAULT_TOKEN=$(vault write -field=token auth/github-jwt/login role=badanie-potok jwt="$GH_TOKEN")
          echo "::add-mask::$VAULT_TOKEN"
          echo "Zalogowano do Vault, token TTL:"
          VAULT_TOKEN="$VAULT_TOKEN" vault token lookup -format=json | grep ttl
        env:
          VAULT_ADDR: https://vault.opscentre.pl
